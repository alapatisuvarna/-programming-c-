#include<iostream>
using namespace std;
int main()
{
  int n,sum=0,m;
  cin>>n;
  int temp=n;
  while(n>0)
  {
    m=n%10;
    sum=sum+(m*m*m);
    n=n/10;
  }
  if(sum==temp)
  {
    cout<<temp<<" is a narcissistic number";
  }
  else
    cout<<temp<<" is not a narcissistic number";
  return 0
