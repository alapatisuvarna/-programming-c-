#include <iostream>
#include<numeric>
using namespace std;
int main()
{
  int arr[]={2, 4, 6, 8};
  int size=4;
  int sum=0;
  sum=accumulate(arr,arr+size,sum);
  cout<<"The sum of the elements in the array:"<<sum;
}
