#include<iostream>
using namespace std;
int gcd(int n1,int n2)
{
  int a=24,b=30,gcd;
  return gcd;
}
int main()
{
  int n1=24,n2=30,i,gcd;
  for(i=1;i<=n1&&i<=n2;i++) 
    if(n1%i==0&&n2%i==0)
      gcd=i;
  cout<<"gcd of two numbers:"<<gcd;
  return 0;
}
