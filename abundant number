#include<iostream>
using namespace std;
int main()
{
  int n,i,sum;
  cin>>n;
  int temp=n;
  for(i=1;i<n;i++)
  {
    if(n%i==0)
    {
      sum=sum+i; 
    }
  }
  if(sum>temp)
    cout<<temp<<" is a abundant number";
  else
    cout<<temp<<" is not a abundant number";
  return 0;
}
