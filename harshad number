#include<iostream>
using namespace std;
int main()
{
  int n,temp,sum=0,m;
  cin>>n;
  temp=n;
  while(n>0)
  {
    m=n%10;
    sum=sum+m;
    n=n/10;
  }
  if(temp%sum==0)
  {
    cout<<temp<<"is a harshad number";
  }
  else
    cout<<temp<<"is not a harshad number";
}
